<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC '-//ibatis.apache.org//DTD Sql Map 2.0//EN' 'http://ibatis.apache.org/dtd/sql-map-2.dtd'>
<sqlMap namespace="test">
	<typeAlias alias="userDto" type="login.user.bean.UserInfoDataBean" />
	
	<select id="loginCheck" resultClass="userDto" parameterClass="userDto">
		select * from userInfo;
	</select>
	
	<!-- 로그인 한 사용자의 정보 보기 -->
	<select id="getUserInfo" resultClass="userDto" parameterClass="String">
		select * from userInfo where id = #id#;
	</select>
	
	<!-- 회원 정보 수정 -->
	<update id="userInfoUpdate" parameterClass="userDto">
		UPDATE userInfo SET 
			name = #name#,
			birth = #birth#,
			phone = #phone#,
			grade = #grade#,
			address = #address#,
			email = #email#
		WHERE id = #id#;
	</update>
	
	
	
	
</sqlMap>







